import UserContextProvider from "../../contexts/userContextProvider.tsx";

export default function AccoPage()
{
    //the home page
    return(
        <UserContextProvider>

        </UserContextProvider>
    );
}